<h1>Rachel's Comment Page</h1>
<div class="parent">
	<h3><%= comment.text %></h3>
	<%= link_to "Edit", edit_comment_path(comment), class: "edit-comment" %>
	<%= link_to "Delete", comment_path(comment), method: "delete" %>
</div>
